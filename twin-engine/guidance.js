import * as THREE from 'https://unpkg.com/three@0.169.0/build/three.module.js';
export function installGuidance(twin,host){const g=new THREE.Group();g.name='guidance';twin.scene.add(g);let note=null;function clear(){while(g.children.length)g.remove(g.children[0]);note?.remove();note=null}function zone(position=[20,.4,12],size=[24,.7,14],label='Zona de trabajo'){clear();const geo=new THREE.BoxGeometry(...size),mat=new THREE.MeshBasicMaterial({color:0x49e7db,transparent:true,opacity:.24,depthWrite:false}),box=new THREE.Mesh(geo,mat);box.position.fromArray(position);const edges=new THREE.LineSegments(new THREE.EdgesGeometry(geo),new THREE.LineBasicMaterial({color:0xc9fff9}));edges.position.copy(box.position);g.add(box,edges);note=document.createElement('div');note.textContent=label;note.style.cssText='position:absolute;top:92px;left:50%;transform:translateX(-50%);z-index:90;padding:8px 12px;border-radius:999px;background:rgba(5,25,33,.9);color:#fff;font:800 11px system-ui';host.appendChild(note);twin.setView?.('front')}twin.highlightMaintenanceZone=zone;twin.clearGuidance=clear;return{zone,clear,dispose(){clear();twin.scene.remove(g)}}}
